---
interface Product {
  key: string;
  name: string;
  url: string;
  status: 'active' | 'excluded';
  excluded_reason?: string;
}

interface ExcludedData {
  products: Product[];
}

interface Props {
  data: ExcludedData;
}

const { data } = Astro.props;
const excludedProducts = (data.products ?? []).filter((p) => p.status === 'excluded');
---
{excludedProducts.length > 0 && (
  <section class="ra-excluded">
    <h3>Why these aren't included</h3>
    <ul>
      {excludedProducts.map((p) => (
        <li>
          <a href={p.url} target="_blank" rel="noopener noreferrer">{p.name}</a>
          {' '}&mdash; {p.excluded_reason}
        </li>
      ))}
    </ul>
  </section>
)}

<style>
  /* Annex from the matrix above with a top rule + breathing room so the
     section reads as supplementary context, not a fifth-tier feature. */
  .ra-excluded {
    margin: var(--space-2xl) 0 0;
    padding-top: var(--space-lg);
    border-top: 1px solid var(--color-border);
    color: var(--color-text-muted);
    max-width: 42rem;
  }

  .ra-excluded h3 {
    font-family: var(--font-heading);
    font-size: var(--font-size-xs);
    font-weight: 600;
    color: var(--color-text-muted);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin: 0 0 var(--space-sm);
  }

  .ra-excluded ul {
    margin: 0;
    padding-left: 1.4rem;
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .ra-excluded li {
    font-size: var(--font-size-sm);
    color: var(--color-text-muted);
    line-height: var(--line-height-body);
  }

  .ra-excluded a {
    color: var(--color-accent);
  }

  :global([data-contrast="high"]) .ra-excluded a {
    text-decoration: underline;
  }
</style>
