# Follow-up: session 2026-04-22

Work from this session needs testing, browser review, and scheduling decisions. Organized by what kind of follow-up is needed.

---

## Browser testing — implemented features

Four features were implemented and the build passes. Each needs a browser walkthrough before the work is considered done.

### Feature 1: `/methodology`
- [ ] Read through the page — does the voice match the site? Does it cover the actual pipeline steps accurately?
- [ ] Check the link to `/scope` resolves
- [ ] Check readable at narrow viewport

### Feature 5: Confidence inline explainer (claim detail pages)
- [ ] Open a claim page (e.g., `/claims/anthropic/publishes-sustainability-report`)
- [ ] Expand the confidence `<details>` — does the copy make sense for this specific claim?
- [ ] Verify the collapsed state looks the same as the old confidence span (flex row not broken)
- [ ] Check on mobile

### Feature 6: Source type badges
- [ ] Open several source detail pages and spot-check `source_type` classification
  - One Anthropic source (should be `primary`)
  - One news article (should be `secondary`)
  - One advocacy org source (should be `tertiary`)
- [ ] Check badge appears before the `kind` badge in the header
- [ ] Check tooltip appears on hover
- [ ] Review a handful of the 146 backfilled files for classification accuracy — especially mixed cases (e.g., IBM, UNESCO, SEC filings)

### Feature 9: `/scope`
- [ ] Read through — does it accurately reflect what the site actually covers?
- [ ] Add or remove scope items if anything is wrong
- [ ] Check the link to `/methodology` resolves

---

## Content review — draft copy on new pages

Features 1 and 9 were written by an agent based on observed codebase behavior. The content is a first draft, not editorial copy. Review specifically:

- [ ] `/methodology` — are the three confidence level descriptions accurate to how the pipeline actually works? Is the `dr review` description correct?
- [ ] `/scope` — are the out-of-scope items the ones you actually intend? Any missing carve-outs?
- [ ] Both pages — does the brand voice feel right? Nothing too soft or explanatory?

---

## Plan review — new plans need scheduling decisions

Three plans were written and are sitting in the backlog as "not started." Each needs a scheduling decision.

### `docs/plans/public-participation-forms.md` (Phase 6.4–6.6)
Covers: Claim Challenge form, Request a Claim form, Propose a Standard form.
- [ ] Read the plan — does the form field spec match what you'd actually want?
- [ ] The plan extends the Phase 6 backend (Cloudflare Workers + D1 + Turnstile). Phase 6 must ship first. Is Phase 6 the right next backend milestone?
- [ ] Decision needed: include 6.4–6.6 in the Phase 6 implementation sprint, or treat as a later add-on?

### `docs/plans/feature-10-audit-trail.md` (Phase 4.8)
Covers: AI Research Audit Trail — sidecar `.audit.yaml` files per claim, pipeline write, Astro loader, collapsible UI.
- [ ] Read the plan — especially the five judge-imposed conditions in the Scope decisions section
- [ ] Open question still in the plan: loader path configurability if/when `research/` splits to its own repo. Low urgency but worth noting.
- [ ] Decision needed: schedule 4.8.1 before or after 4.6 (pipeline perf hardening)?

---

## Scheduling decisions

| Item | Blocks | Decision needed |
|------|--------|-----------------|
| Phase 4.6 (pipeline perf) | — | Is this the next coding sprint? |
| Phase 4.8.1 (audit trail Phase 1) | Needs pipeline work (persistence.py) | Before or after 4.6? |
| Phase 6 (public feedback) | — | When does backend work start? |
| Phase 6.4–6.6 (participation forms) | Requires Phase 6 backend | Bundle with 6.1–6.3 or after? |

---

## Methodology and scope pages — nav/discoverability

The new `/methodology` and `/scope` pages have no inbound links from the main nav (intentional — they're utility pages). Current plan: link from `/methodology` ↔ `/scope` cross-link, and surface in footer.

- [ ] Add `/methodology` and `/scope` to the footer alongside the TreadLightly and license links
- [ ] Confirm the cross-links between the two pages are in place

---

## Source type classification — edge cases to revisit

The 146-file backfill used publisher-name heuristics. A few categories may need a second pass:

- **SEC EDGAR filings** — classified as `primary` (correct: they're the filing authority), but the *subject* of the filing is the company. Verify this is the right call.
- **B Lab / B Corp profiles** — classified as `secondary` (correct: third-party certification body). Confirm.
- **UNESCO, NTIA, UNFCCC** — classified as `secondary` (government/IGO, independent from subject). Confirm.
- **IBM, Deloitte reports** — classified as `secondary` (industry analysis). Some may be vendor-published content that leans tertiary. Spot-check.
