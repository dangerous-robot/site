# v1.0.0 Roadmap

**Status**: In progress — sprint planning doc  
**Last updated**: 2026-05-01  
**Working version**: `1.0.0-alpha.2` (per `VERSION.md`)  
**Detail research**: [`plans/drafts/v0.1.0-mvp-definition.md`](plans/drafts/v0.1.0-mvp-definition.md)

> **Renamed 2026-04-26.** Previously `docs/plans/v0.1.0-roadmap.md`; the first public release will be `v1.0.0`. Sub-plan filenames containing `v0.1.0` (e.g., `v0.1.0-vocab-workflow-landing.md`, `drafts/v0.1.0-mvp-definition.md`) keep their original names.

Status key: `done` | `in progress` | `ready` (plan exists, not started) | `plan needed` | `decision needed`

---

## Scope decisions resolved 2026-04-27

Six items raised in the roadmap pass earlier today were resolved:

- **In v1.0.0**: `dr publish`, `dr review-queue` Phase 1, publish-time `criteria_slug` gate (+ frontmatter back-reference), `parent_company` schema field. Folded into the release-criteria checklist below.
- **Post-v1**: `criterion-resolution-workflow_stub.md` (workflow follow-on; stub-stage, untracked).
- **Confirmed intentional**: lint severity asymmetry — `published-without-criterion` is `error`, `published-without-review` is `warning` until GA. Criterion is a join key so missing it should block publication; review is a quality bar that only hardens at GA.

---

## Scope-creep reconciliation 2026-04-28

Reconciles roadmap with work landed since the 2026-04-27 pass. No release-criteria items deferred; this round is mostly additions and completions plus a few items needing Brandon's call.

**Completed since last roadmap touch:**
- Footer site version display (commit `41285a4`) — closes the open §8 decision.
- Hero homepage redesign: scattered claim cards with build-time randomized positions (commit `b84f204`), replacing the prior filmstrip ticker.
- `tags: string[]` claim schema field with reserved `highlight` tag (commit `90a5c2c`); a curated subset of published claims is tagged for the homepage scatter.

**Newly added to v1 scope (shipped):**
- Researcher decomposition: 3-step tool-free pipeline (planner → search executor → scorer), now the default `researcher_mode`. Commits `0f2a33d`, `211b752`, `6409918`. Plan: [`plans/researcher-decomposition.md`](plans/researcher-decomposition.md). Adds `llm_concurrency` semaphore and decomposed-mode internals to `docs/architecture/`.
- Per-agent model overrides + per-agent `models_used` in audit sidecars (commits `54702c8`, `3abd112`, `6409918`). Sidecar now also carries a `research:` block (planner queries+rationale, scorer rationale, counts) and analyst diagnostics (`capture_run_messages`, `logger.exception`).
- `dr archive` / interim wayback default flip: `skip_wayback=False` is now the in-pipeline default until the background-job framework lands. Plan: [`plans/wayback-archive-job.md`](plans/wayback-archive-job.md). The plan documents the revert path; this is intentionally interim, not a v1 release blocker.
- `llm-tester` promoted from gitignored POC to tracked scripts (commits `244ac22`, `e0a4501`). Used to validate `gpt-oss-120b` (T1–T5 all pass per commit `ae50419`).
- Microsoft entity + sources onboarded (commit `e218781`). Microsoft claim slugs are still in flux in the working tree; settle slugs before tag.
- Onboarded TreadLightlyAI as a product entity with COI claims, audit sidecars, and 2021–2026 sources (commit `e218781`); reverses the earlier 2026-04-26 deletion noted in the criteria checklist.

**Open reconciliation note:**
- **Multi-provider Part 2 promoted into v1 scope.** Per-agent tiering shipped in commit `54702c8` (2026-04-27); the verification run surfaced gateway findings that reshaped the recommended agent/model mapping. §12 has been updated to cover Parts 1 and 2 jointly. Part 3 (fallback + second provider) remains post-v1.

---

## Scope-creep reconciliation 2026-04-29

SEO audit implemented across three plans ([`plans/seo-technical.md`](plans/seo-technical.md), [`plans/seo-copy.md`](plans/seo-copy.md), [`plans/seo-external.md`](plans/seo-external.md)).

**Completed (code shipped):**
- `robots.txt` in `public/`; `@astrojs/sitemap` with per-page-type priority overrides; `trailingSlash: "never"`
- Canonical URL, Open Graph tags, `ogImage?` prop, and Organization JSON-LD in `Base.astro`
- Async (non-blocking) font preload for Orbitron
- BreadcrumbList, ClaimReview, FAQPage, and WebSite JSON-LD schemas — all live in component/page templates
- Per-page-type title templates: entity (`{Name} AI {Type} Claims`), topic (`AI {Label} Claims`), criteria (placeholder substitution), source (conditional publisher suffix)
- Per-page-type meta descriptions for all dynamic page types
- `seo_title` optional field on claim schema (`z.string().max(42).optional()`); used in `claims/[...slug].astro`
- `missing-seo-title` lint check (info severity, published claims only); `seo_title` added to `CANONICAL_CLAIM_KEYS`
- Homepage title duplication bug fixed (`"AI Transparency Research"` passed to layout)

**Not implemented (confirmed non-goals):**
- Twitter/X card tags

**Gated on launch (external plan):**
- Google Search Console setup, sitemap submission, Fact Check program application — all in `seo-external.md`, blocked on alpha banner removal and meaningful published-claim corpus

---

## Scope-creep reconciliation 2026-05-01

Major content sprint since the 2026-04-29 roadmap pass. No release-criteria items deferred; this round onboards four entities that were either deleted or never present in the original flagship list.

**Completed since last roadmap touch:**
- `brave-leo` (product) onboarded — 6 claims, 5 published, 1 blocked (`excludes-frontier-models`). All 6 have audit sidecars; 6 have human signoff.
- `brave-software` (company) onboarded — 4 claims, 2 published, 2 blocked (`corporate-structure`, `publishes-sustainability-report`). 2 published claims have sidecars and signoff.
- `chatgpt` (product) onboarded — 7 claims, 6 published, 1 draft (`excludes-frontier-models`). All 7 have sidecars; 6 published have signoff.
- `microsoft` (company) onboarded (commit `e218781` area) — 4 claims, all published, all have sidecars and signoff.
- `openai` (company) onboarded — 4 claims, all published, all have sidecars and signoff.
- `anthropic` (company) — 1 draft claim (`donates-to-environmental-causes`), no sidecar yet.

**Entities removed since last roadmap touch:**
- `treadlightlyai`, `google` (no claims), `claude` claims, `gemini` claims — all deleted or left empty. `claude` and `gemini` entity files remain under `research/entities/products/` but have no claims on disk.

**Plan housekeeping (this pass):**
- 12 completed plan files moved to `docs/plans/completed/`.
- `docs/plans/v1-launch-set.md` relocated to `research/v1-launch-set.md` to match roadmap link and internal paths.
- `sector-claims.md`, `multi-provider.md`, `dr-review-queue.md`, `wayback-archive-job.md` status headers updated to reflect partial completion.

---

## Release criteria checklist

The site can be tagged `v1.0.0` when all of these are true:

- [x] Criteria curated to 6-template active set (3 company, 3 product) in `templates.yaml`
- [x] "Standards" → "Criteria" rename live (display + schema + route) — `/criteria/` route, `criteria` collection, `criteria_slug` field all landed
- [x] All existing claims backfilled to `status: published` (17 files, 2026-04-22)
- [x] `status` field schema implemented and pipeline writes `status: draft` on new claims — schema at `content.config.ts:160`, pipeline writes draft in `persistence.py`
- [x] Curated claim set published: Earthly Insight removed ✓, TreadLightlyAI COI claims deleted ✓. ChatGPT description sub-item is **stale** (2026-04-26): `products/chatgpt.md` no longer exists; only `claude.md` and `gemini.md` remain under `products/`. Underlying onboarding bug captured separately under "Follow-ups noted during content & disclosure pass" below.
- [x] `existential-safety-score` moved to sector scope — now under `sectors/ai-llm-producers`
- [x] COI disclosure on FAQ — landed as `#conflicts-of-interest` accordion in `src/pages/faq/index.astro` (no standalone `/methodology` page; methodology lives in the FAQ accordion)
- [x] ~~COI notice on `companies/treadlightlyai` entity page~~ — **stale** (2026-04-26): `companies/treadlightlyai.md` was deleted user-authorized in the v1.0.0 prep pass. COI disclosure now lives only on the FAQ; if a treadlightlyai entity is reintroduced, the inline notice item resurrects with it.
- [x] GitHub repo correctly linked in footer
- [x] README expanded to be minimally useful to an external visitor — now describes the project, what's in each top-level dir, dev commands, contributing paths, COI pointer, license
- [x] At least one GitHub issue template live — `.github/ISSUE_TEMPLATE/submit-source.md` (source submission)
- [x] `dr lint` CI integration job in `ci.yml` — landed as `lint-content` job in `.github/workflows/ci.yml`
- [x] `dr lint` `published-without-review` rule flipped from `warning` → `error` at `1.0.0-beta.1` (`pipeline/linter/checks.py:117,127`).
- [x] `dr publish` bulk auto-publish (commit `58e7c00`) — flips drafts to `published` for the launch-set workflow without per-claim review. Pairs with the publish-time criterion gate below.
- [x] Publish-time `criteria_slug` gate — `published-without-criterion` lint rule (`error` severity) plus `dr publish` skip-on-missing; pipeline writes `criteria_slug` back into claim frontmatter (commit `b2be74e`). Severity asymmetry vs. `published-without-review` is intentional (see "Scope decisions resolved" above).
- [x] `parent_company` entity field — schema landed in `src/content.config.ts`; in use on `claude.md` and `gemini.md`. Inference logic ([`plans/parent-company-inference.md`](plans/parent-company-inference.md)) is post-v1; only the schema field is a v1 line item.
- [x] `dr review-queue` Phase 1 ([`plans/dr-review-queue.md`](plans/dr-review-queue.md)) — operator command for walking draft claims awaiting sign-off. Landed in commits `f64adc3` + `3abd112`: `pipeline/orchestrator/review_queue.py`, `pipeline/orchestrator/review.py`, `pipeline/common/sidecar.py`, 18 tests in `pipeline/tests/test_review_queue.py`. Interactive a/s/p/o/q loop; `--format text|json` for CI; `approve_claim` callable shared with `dr review`.
- [x] Pre-launch quick fixes plan complete ([`plans/pre-launch-quick-fixes.md`](plans/pre-launch-quick-fixes.md)) — closed out 2026-04-27: P2 (`dr research` → `dr verify-claim`) and S3 (COI on `treadlightlyai`) landed; S5 (pipeline diagram) bumped to stretch; ST5 (confidence rubric on `/methodology`) dropped (no `/methodology` page).
- [x] Multi-provider Part 1: one `dr verify` demo run against Infomaniak `gpt-oss-120b` ([`plans/multi-provider.md#part-1----v1-release-switch-to-infomaniak-gpt-oss-120b`](plans/multi-provider.md#part-1----v1-release-switch-to-infomaniak-gpt-oss-120b)), verified 2026-04-26 via `dr onboard anthropic`; all four agents executed against `infomaniak:openai/gpt-oss-120b`.
- [x] Multi-provider Part 2: per-agent model selection with Mistral scrubber ([`plans/multi-provider.md#part-2----two-infomaniak-models-per-agent-preference`](plans/multi-provider.md#part-2----two-infomaniak-models-per-agent-preference)), shipped 2026-04-27 in commit `54702c8`. Mixed-tier mapping documented in the plan; gateway findings limit Infomaniak to analyst+auditor today.
- [x] Researcher decomposition: planner → search executor → scorer pipeline, tool-free LLM steps, default `researcher_mode` (commits `0f2a33d`, `211b752`, `6409918`). Plan at [`plans/researcher-decomposition.md`](plans/researcher-decomposition.md). Adds `llm_concurrency` semaphore for parallel onboard sessions.
- [x] Per-agent model overrides + per-agent `models_used` in audit sidecar; `research:` block (planner queries+rationale, scorer rationale, counts) and analyst diagnostics added to sidecar (commit `6409918`).
- [x] Footer site version row reading `VERSION.md` at build time (commit `41285a4`) — closes the §8 open decision.
- [x] Hero homepage: scattered claim cards with build-time randomized positions (commit `b84f204`); replaces filmstrip ticker.
- [x] `tags: string[]` claim schema field with reserved `highlight` tag (commit `90a5c2c`); a curated subset of published claims is tagged for the homepage scatter. AGENTS.md documents the reserved-tag convention.
- [x] `llm-tester` promoted from gitignored POC to tracked scripts (commits `244ac22`, `e0a4501`); `gpt-oss-120b` T1–T5 all pass (commit `ae50419`). Plan: [`plans/llm-tester-refactor.md`](plans/llm-tester-refactor.md).
- [~] Wayback archival as a background-job framework — **punted to v1.1** ([`docs/v1.1.0-roadmap.md §2`](v1.1.0-roadmap.md)). Interim `skip_wayback=False` default in-pipeline (commit `6409918`) remains until the framework ships; revert path documented in [`plans/wayback-archive-job.md`](plans/wayback-archive-job.md).
- [x] Pipeline fail-fast for 403/401 (`TerminalFetchError`) — already implemented
- [x] Timeout centralization (`pipeline/common/timeouts.py`) — already implemented
- [x] Source dedup in onboard (`onboard-reuse-verify-sources`) — already implemented
- [x] Workflow paragraphs and design principle landed in `AGENTS.md` and `glossary.md` (commit `7943577`)
- [x] Orchestrator + Router documented in `glossary.md` Roles + Workflow + Agent-tasks tables (Item 3 (a) split) (commit `7943577`)
- [x] `docs/plans/triage-agent.md` promoted from drafts (2026-04-26); V1 surface implementation done (commit `020409f`): Orchestrator docstring naming + threshold-in-Orchestrator wiring. Full Router still post-v1.
- [x] `docs/plans/claim-lifecycle-states.md` promoted from drafts (2026-04-26); implementation done (commit `df7537e`): `phase` field, `blocked_reason` field, `'blocked'` status enum, `< 4` threshold (planned as `>= 2`; code landed at `< 4`).
- [x] `docs/plans/multi-topic.md` promoted from drafts (2026-04-26); schema and code implementation done (commit `1394bc6`): `category` → `topics` rename, array cardinality (1–3), templates strawman in place, acceptance tests skipped via marker. Operator regenerated the launch claim set 2026-04-27 (tracked in [`research/v1-launch-set.md`](../research/v1-launch-set.md)).
- [x] Object lifecycle (general four-state) documented in `glossary.md` (commit `7943577`)
- [x] Updated claim lifecycle (`in-progress`, `blocked`, threshold `< 4`) documented in `glossary.md` (commit `7943577`)

v1.0.0 is **not** blocked by: audit trail UI (append-only recheck history), public feedback backend, pipeline diagram, multiple external reviewers, or audit-trail CI gates (staleness + orphan checks — tracked under audit-trail-extensions Stage 2 in [`UNSCHEDULED.md`](UNSCHEDULED.md)).

### Follow-ups noted during content & disclosure pass (2026-04-26)

- **Empty-description onboarding edge case.** When `dr onboard` runs with a seed source whose `summary` is empty (e.g. a fail-fast 401/403 fetch, or a body the Ingestor cannot summarize), `pipeline/orchestrator/pipeline.py` lines 640 + 657 leave `entity_description = ""` with no fallback. The created entity then has `description: ''` until an operator edits it by hand. Tracked in UNSCHEDULED.md; not a v1 blocker because no current entity is in this state.

---

## 0. Criteria curation

**Status**: `done`  
**Detail**: [`plans/drafts/v0.1.0-mvp-definition.md §0`](plans/drafts/v0.1.0-mvp-definition.md)

`research/templates.yaml` already has 6 active templates (3 company, 3 product) and 13 deferred under `inactive_templates`. No code changes needed.

---

## 1. "Standards" → "Criteria"

**Status**: `done` (landed in commit 3282079, "criteria rename Phase 1")  
**Plan**: deleted 2026-04-27 (`docs/plans/criteria-rename_stub.md` no longer tracked; rename is fully shipped).

`/criteria/` route exists, `criteria` collection registered in `src/content.config.ts:207`, `criteria_slug` field in place. Schema-level rename complete. Pipeline now writes `criteria_slug` back into claim frontmatter (commit `b2be74e`). Any residual `standard_slug` references in claim bodies should be caught by `dr lint`.

---

## 2. Claim status / release flag

**Status**: `done` (landed in commits 73f40d2, 9d242ab)  
**Plan**: [`plans/claim-status_stub.md`](plans/claim-status_stub.md)

Schema has `status: z.enum(['draft', 'published', 'archived']).default('draft')` (`content.config.ts:160`). Verdict enum extended with `not-applicable`. All 17 existing claims backfilled to `status: published`. Pipeline writes `status: draft` on new claims. Verify before tagging: public list pages actually filter to `published` and direct URLs render non-published claims with a status notice.

---

## 3. Curated v1.0.0 claim set

**Status**: `in progress` — set tracked in [`research/v1-launch-set.md`](../research/v1-launch-set.md)
**Source of truth**: the launch-set file above.

As of 2026-05-01, active entities on disk are: `brave-leo` (product, 6 claims: 5 published, 1 blocked), `brave-software` (company, 4 claims: 2 published, 2 blocked), `chatgpt` (product, 7 claims: 6 published, 1 draft), `microsoft` (company, 4 claims: all published), `openai` (company, 4 claims: all published), `anthropic` (company, 1 draft claim). Entity files exist for `claude` (product) and `gemini` (product) but neither has claims on disk yet. The `treadlightlyai` entity and all google/gemini/claude claims from earlier sprints are no longer present.

Lint rule `published-without-review` stays `warning` pre-GA and escalates to `error` at v1 final.

**Remaining**: see [`research/v1-launch-set.md` § Gaps to close before tag](../research/v1-launch-set.md). Open items include resolving every `target: tbd` row, deciding reviewer-signoff policy pre-tag, and deciding whether to onboard claude/gemini claims before the v1 tag.

---

## 4. Sector-wide claims

**Status**: Partially done — schema change complete, content pending  
**Plan**: [`plans/sector-claims.md`](plans/sector-claims.md)

`sector` added to entity `type` enum (commit 33c8ad3). As of 2026-05-01, no `research/entities/sectors/` directory or sector claim files exist on disk. The `/sectors/` route and sector claim content are post-v1. A separate agent is handling this plan.

---

## 5. Human sign-off

**Status**: `done` (v1.0 minimum met; remaining items punted to v1.1)  
**Plans**: [`plans/drafts/human-signoff-research.md`](plans/drafts/human-signoff-research.md), [`plans/drafts/human-signoff-identity-research.md`](plans/drafts/human-signoff-identity-research.md), [`plans/audit-trail.md`](plans/audit-trail.md), [`plans/claim-promotion-audit-rename-verdicts.md §1`](plans/claim-promotion-audit-rename-verdicts.md)

**Done (v1.0 minimum)**:
- [x] `dr review --approve` / `--archive` implemented (claim-promotion plan §1)
- [x] GitHub issue template for source submission (`.github/ISSUE_TEMPLATE/submit-source.md`)
- [x] COI disclosure on FAQ (`#conflicts-of-interest` accordion)

**Punted to v1.1** ([`docs/v1.1.0-roadmap.md`](v1.1.0-roadmap.md)):
- [ ] GitHub issue template for claim requests (`.github/ISSUE_TEMPLATE/request-claim.md`)
- [ ] ORCID OAuth for external reviewer identity
- [ ] IndieAuth, Mastodon/Bluesky as additional identity options
- [ ] Public-participation-forms plan: web forms for source submission, claim requests, standard proposals

---

## 6. Pipeline diagram

**Status**: `punted to v1.1` ([`docs/v1.1.0-roadmap.md §3`](v1.1.0-roadmap.md))  
**Plan**: [`plans/pipeline-diagram_stub.md`](plans/pipeline-diagram_stub.md)

Static SVG or Mermaid flowchart of the agent pipeline for the FAQ accordion. FAQ text covers it adequately for v1.0.

---

## 7. Fit and finish

**Status**: `in progress`  
**Detail**: Full 13-item checklist in [`plans/drafts/v0.1.0-mvp-definition.md §7`](plans/drafts/v0.1.0-mvp-definition.md)

**Done since last revision** (2026-04-26):
- COI disclosure: `#conflicts-of-interest` accordion in `src/pages/faq/index.astro`.
- README expanded: project description, top-level dirs, dev commands, contributing paths, COI pointer, license.
- Source-submission issue template: `.github/ISSUE_TEMPLATE/submit-source.md`.
- `chatgpt` entity description: obsolete — `products/chatgpt.md` was deleted in the v1.0.0 prep pass.

**Punted to v1.1.0** ([`docs/v1.1.0-roadmap.md §1`](v1.1.0-roadmap.md)):
- [ ] Claim-request issue template: `.github/ISSUE_TEMPLATE/request-claim.md`
- [ ] CONTRIBUTING.md — sections for source submission, claim requests, `dr` CLI overview
- [ ] Footer: license link, methodology/FAQ link, feedback/contact link
- [ ] `/topics` rendering verification
- [ ] Source submission mailto fallback for users without GitHub

**Done**:
- [x] Per-page `description` meta tags on list pages — done as part of SEO audit (2026-04-29): dynamic descriptions on all page types, plus title templates

---

## 9. Content linter (`dr lint` static scan)

**Status**: `done` — covered by pre-commit hook; no separate pre-tag action required.  
**Plan**: [`plans/dr-lint.md`](plans/dr-lint.md)

---

## 10. Claim promotion, `dr reassess` rename, verdict definitions

**Status**: `done` (landed in commit `73f9360 feat: dr review promotion, dr reassess rename, sharpen verdict defs`)
**Plan**: [`plans/claim-promotion-audit-rename-verdicts.md`](plans/claim-promotion-audit-rename-verdicts.md)

- §1 Claim promotion: `dr review --approve` / `--archive` flags in `pipeline/orchestrator/cli.py`; tests in `pipeline/tests/test_audit_trail.py`.
- §2 Rename: `dr reassess` is the canonical function name; `AGENTS.md`, `research-workflow.md`, and the CLI smoke test all aligned.
- §3 Verdict definitions: tightened in `docs/architecture/glossary.md`, `pipeline/analyst/instructions.md`, `pipeline/auditor/instructions.md`.

The first `.audit.yaml` sidecar will be produced when `dr review --approve` runs against a regenerated claim — claim regeneration is the operator action remaining for multi-topic (release criteria, line 41).

---

## 11. Pre-launch quick fixes

**Status**: `done` (closed out 2026-04-27 — see plan for per-item disposition)
**Plan**: [`plans/pre-launch-quick-fixes.md`](plans/pre-launch-quick-fixes.md)
**Triage source**: [`pre-launch-triage.md`](pre-launch-triage.md) (dispatch ledger; deletable once destinations are reachable)

A bundle of small reader-facing and operator-vocabulary fixes from the 2026-04-24 pre-launch triage. Each item is shippable in under a day. Highlights:

- ALPHA banner; `/values` editorial page; pipeline diagram on FAQ/methodology
- Audit sidecar `models_used` field (lock schema **before** the first sidecar is written)
- Reader-takeaway line under each verdict badge
- Rename "Citation Auditor" → "citation check"; rename `dr research` → `dr verify-claim`
- Remove "Page Builder" role (operator: causing confusion)
- Document model-tier discipline; confidence rubric on `/methodology`

Larger items from the same triage have their own plans:

- [`plans/v0.1.0-vocab-workflow-landing.md`](plans/v0.1.0-vocab-workflow-landing.md) — full vocabulary cohesion pass (absorbed and supersedes the deleted `vocab-rename-pass_stub.md`)
- [`plans/acceptance-test-fixture_stub.md`](plans/acceptance-test-fixture_stub.md) — Anthropic/Claude regression case (Q8)
- [`plans/source-trust-metadata_stub.md`](plans/source-trust-metadata_stub.md) — minimum subset for launch sources
- [`plans/triage-agent.md`](plans/triage-agent.md), [`plans/claim-lifecycle-states.md`](plans/claim-lifecycle-states.md), [`plans/multi-topic.md`](plans/multi-topic.md) — v1.0.0-required schema and workflow plans (promoted from drafts 2026-04-26)

Open questions live at [`pre-launch-questions.md`](pre-launch-questions.md).

---

## 12. Multi-provider Parts 1 & 2 (Infomaniak `gpt-oss-120b` + per-agent tiering)

**Status**: `done` (Part 1 verified 2026-04-26; Part 2 implemented 2026-04-27 in commit `54702c8`. Part 3 remains post-v1.)
**Plan**: [`plans/multi-provider.md`](plans/multi-provider.md) (Parts 1 and 2)
**Report**: [`reports/API-PROVIDER-FINAL-REPORT.md`](reports/API-PROVIDER-FINAL-REPORT.md)

Part 1 added `infomaniak:` model-spec support so `dr verify` can target Infomaniak-hosted `gpt-oss-120b`; the canned-claim demo ran 2026-04-26 with all four agents producing a written `.audit.yaml`.

Part 2 added per-agent model selection (`--researcher-model` / `--analyst-model` / `--auditor-model` / `--ingestor-model` plus matching `DR_*_MODEL` env vars) and a Mistral `reasoning_content` scrubber wired through `OpenAIModelProfile`. The mixed-tier verification surfaced two model+gateway findings (recorded in the plan): `gpt-oss-120b` emits tool-call intent in `reasoning` instead of `tool_calls`, restricting it to analyst/auditor; `gemma3n` cannot do tool calling on Infomaniak today. The practical recommended mapping is therefore an Anthropic baseline (researcher + ingestor) with Infomaniak structured-output agents (analyst + auditor); the original "all four agents on Infomaniak" mapping is invalidated until either Infomaniak fixes serialization or the tool-free researcher/ingestor variants land (tracked separately under [`plans/drafts/tool-free-researcher-ingestor_stub.md`](plans/drafts/tool-free-researcher-ingestor_stub.md)).

Motivation for Infomaniak-first (not GreenPT, despite the report's marginal preference): Infomaniak's data centres reuse waste heat to warm Swiss buildings, a direct fit for TreadLightly's environmental-transparency brand.

Part 3 (global fallback chain + a second provider, likely GreenPT) is explicitly post-v1.

---

## 13. Result-quality validation

**Status**: `decision needed` — define the v1 quality bar and the smallest test that proves it holds

The site's value depends on whether readers trust verdicts enough to argue with them. We don't currently test that property — `dr lint` checks structure, the audit trail records analyst/auditor agreement, but neither tells us whether a *published* claim is actually defensible. This item is the placeholder for that work; scope is intentionally not nailed down yet, but the v1 GA bar should at minimum specify:

**Major signals to test:**
- **Source quality and appropriateness**, not just `kind`. Today `SourceFile.kind` is one of `report | article | documentation | dataset | blog | video | index` — that's *type*, not *quality*. A press release and a peer-reviewed paper are both `article`. We have no field for primary-vs-secondary, partisan-vs-neutral, evidence-density, or "is this source appropriate to *this* claim." Either we add structured fields or we accept that quality lives only in the analyst's head.
- **Claim text fidelity to the criterion.** LLM-generated claim text is loosely controlled today: the criterion sets the topic, but the analyst may rephrase in ways that drift. Observed bad pattern: `"X is unverified"` rephrased as a verdict-word claim where the verdict then echoes the title (`unverified` → verdict `unverified`). Criterion text should constrain claim text more tightly — either templated (criterion provides the title pattern) or validated (lint rejects claim titles that lexically match the verdict word).
- **Verdict-evidence coherence.** Does the narrative actually cite the sources it claims to? Are cited quotes present in the source bodies? Does the verdict make sense given the cited stances? Today this is implicit in analyst+auditor agreement; an explicit check would catch agreed-but-wrong runs.
- **Confidence calibration.** Across a sample of published claims, does `high` confidence correlate with verdicts a reviewer would also rate `high`? `medium` and `low` similarly? Without sampling, the labels are decorative.

**Key minimums for v1 GA:**
- A documented quality bar — what makes a claim publishable beyond "lint passes and a human approved." Even if the bar is "the four signals above were spot-checked on the launch set," writing it down lets future-us tell whether we're holding the line.
- Claim text constrained by criterion (template, validator, or both). Bans the `unverified → unverified` shape.
- Some structured signal of source appropriateness — at minimum a `primary | secondary | tertiary` field, or a "claim relevance" score the analyst writes per cited source.

**Clarity for readers:**
- The bar we hold ourselves to should be stated on the site (likely in the FAQ alongside COI), so readers know what *not* having that label means about claims we declined to publish.
- The omission policy ("what's worse than publishing imperfectly?") should be operator-facing prose, not implicit in `dr lint` thresholds.

This work is not gating tag-as-1.0.0 — `1.0.0` records content/code readiness — but it is gating the "should this be visible at the public domain" decision. Plan-stub forthcoming.

---

## 8. Semantic versioning

**Status**: `done` — pre-release scheme adopted, footer display landed

`VERSION.md` (commit `f39d564`) establishes pre-release stages: `alpha.N` (open checklist items), `beta.N` (checklist complete, schema frozen), `rc.N` (would-ship candidate), `1.0.0` final. Working version is `1.0.0-alpha.1`. DNS resolution is explicitly not part of the tag criteria — the tag records content/code readiness, not domain availability.

Footer version row landed in commit `41285a4` (reads `VERSION.md` at build time, alongside GitHub + CC-BY-4.0 links).

**Version semantics for a content site** (post-1.0):
- **Major**: breaking change to verdict enum, criterion definitions, or entity URL structure
- **Minor**: new entity type, new criterion, new capability shipped (e.g., audit trail, public feedback)
- **Patch**: content corrections, verdict updates, bug fixes, source additions

---

## 14. Source-403 recovery

**Status**: `plan needed`

Three claims originally stalled due to 403s on `openai.com` / `help.openai.com`. As of 2026-05-01: `openai/publishes-sustainability-report` and `chatgpt/no-training-on-user-data` are now `published`. Only `chatgpt/excludes-frontier-models` remains unresolved (`draft`). The 403-recovery strategies below still apply to this claim and any future claims on auth-walled domains.

The pipeline's `TerminalFetchError` path skips 403/401 URLs immediately with no fallback, leaving fewer than the 2-source minimum.

Two recovery strategies under consideration:

1. **URL scorer blocklist bias**: Deprioritize hosts in `common.blocklist` known to 403 (`openai.com`, `help.openai.com`) so the researcher favors third-party coverage before hitting the wall. Simpler; avoids the 403 entirely for claims with third-party coverage.

2. **Wayback Machine fallback**: When a live fetch returns 403, retrieve a recent archived copy instead of skipping the URL. Broader recovery for claims with no third-party sources, but adds latency and archive-availability dependency. (See also: [`plans/wayback-archive-job.md`](plans/wayback-archive-job.md) for the background-job framing.)

Plan stub forthcoming before implementation begins.

---

## 15. Scorer quality signals

**Status**: `done` (commits `9083746`, `a9d833c`)
**Plan**: [`plans/completed/scorer-quality-signals.md`](plans/completed/scorer-quality-signals.md)

Three targeted improvements to the scorer and query planner, implementable without architectural changes:

- **A**: Fix scorer fallback — when scorer drops all candidates, fail the query rather than silently keeping all candidates
- **B**: Inject publisher quality hints into scorer prompt — per-candidate domain classification (primary/secondary/tertiary) and forum domain soft-block
- **C**: Pass `parent_company` into planner and scorer prompts — field already exists on `ResolvedEntity`, not yet injected

See [`plans/source-quality_survey.md`](plans/source-quality_survey.md) and [`plans/completed/source-quality-do-now_completed.md`](plans/completed/source-quality-do-now_completed.md) for the broader source quality picture; the v1 verification scale + cap landed under §16.

---

## 16. Source quality robust roadmap

**Status**: `done` (commits `0caf560`, `687ab54`, `54c09e6`, `4c15e72`, `4bb800b`, `79d69ca`, `46440c4`, `73fbbb4`, `d3c709c`, `89b6bfc`, `61470fd`, `afc86e2`, `113349c`)
**Plan**: [`plans/completed/source-quality-robust-roadmap_completed.md`](plans/completed/source-quality-robust-roadmap_completed.md)
**Architecture**: [`architecture/source-quality.md`](architecture/source-quality.md)

V1 source-quality scope: a per-claim `verification_level` derived from a per-source `independence` field, a confidence cap at `low` for `claimed`/`self-reported` claims with an analyst-written rationale, and a claim-page display that surfaces the label, gloss, source-count breakdown, and cap rationale to readers.

- **Architecture doc** (`architecture/source-quality.md`): single reference for the verification scale, the `source_type → independence` proxy and its known failure modes, the confidence cap rule, and the per-claim `source_overrides` mechanism.
- **Schema additions**: `independence` on sources; `verification_level`, `cap_rationale`, `source_overrides` on claims (Zod + Pydantic mirrored).
- **Analyst + auditor**: instructions extended with the cap rule, rationale templates, restatement test, and absence-of-evidence editorial policy. Auditor mirrors the cap so independent assessments don't fight a correctly-capped verdict.
- **Lint**: three new checks (missing-independence on recent sources, confidence-cap-violation, missing-cap-rationale).
- **Backfill**: 136 v1 launch sources got `independence`; 23 v1 launch claims were re-run via `dr claim-refresh` with smaller-surface sign-off preservation. 6 unchanged_preserved, 15 changed_cleared, 1 regression to `blocked` (terminal_fetch_error during the re-run on `chatgpt/excludes-image-generation`); per-claim deltas in `plans/source-quality-rerun-log.jsonl`.
- **Display**: claim page renders verification label + plain-language gloss, source-count breakdown by independence, and a cap-rationale block when the cap fires. The `multiply-verified` schema slug displays as "Cross-verified".
- **Deferred to v1.x**: publisher-groups registry; `site_trust` and `publisher_group` fields; `document_type`, `authority`, `coi_with_subject` schema additions; full backfill of all 292 sources; agent classifier for trust fields. Tracked in [`plans/source-trust-metadata.md`](plans/source-trust-metadata.md).

---

## Backlog cross-reference

| Roadmap item | Backlog phase | Plan file |
|---|---|---|
| Criteria rename | — (new) | plan deleted 2026-04-27; rename complete |
| Claim status flag | — (new) | [`plans/completed/claim-status_stub.md`](plans/completed/claim-status_stub.md) ✓ done |
| Curated claim set | — (content work) | this doc |
| Sector claims | — (new) | [`plans/sector-claims.md`](plans/sector-claims.md) (partial) |
| Human sign-off / audit trail | audit-trail plan | [`plans/completed/audit-trail.md`](plans/completed/audit-trail.md) |
| Human sign-off identity | reviewer identity plan | [`plans/drafts/human-signoff-identity-research.md`](plans/drafts/human-signoff-identity-research.md) |
| Public participation forms | public participation forms plan | [`plans/public-participation-forms.md`](plans/public-participation-forms.md) |
| Pipeline perf | source dedup / pipeline hardening | [`plans/completed/onboard-reuse-verify-sources.md`](plans/completed/onboard-reuse-verify-sources.md) ✓ done |
| Public feedback backend | public feedback backend plan | [`plans/public-feedback.md`](plans/public-feedback.md) |
| Fit and finish | — | this doc |
| Pipeline diagram | — (design, deferred) | [`plans/pipeline-diagram_stub.md`](plans/pipeline-diagram_stub.md) |
| Content linter | — (new) | [`plans/dr-lint.md`](plans/dr-lint.md) |
| Claim promotion / `dr reassess` rename / verdict defs | — (new) | [`plans/completed/claim-promotion-audit-rename-verdicts.md`](plans/completed/claim-promotion-audit-rename-verdicts.md) ✓ done |
| Pre-launch quick fixes | — (new) | [`plans/completed/pre-launch-quick-fixes.md`](plans/completed/pre-launch-quick-fixes.md) ✓ done |
| Vocabulary + workflow landing | — (new) | [`plans/completed/v0.1.0-vocab-workflow-landing.md`](plans/completed/v0.1.0-vocab-workflow-landing.md) ✓ done |
| Router + Orchestrator naming | — (new) | [`plans/completed/triage-agent.md`](plans/completed/triage-agent.md) ✓ done (v1 surface) |
| Claim lifecycle states | — (new) | [`plans/completed/claim-lifecycle-states.md`](plans/completed/claim-lifecycle-states.md) ✓ done |
| Multi-topic schema rename | — (new) | [`plans/completed/multi-topic.md`](plans/completed/multi-topic.md) ✓ done |
| Acceptance test fixture (Anthropic/Claude) | — (new) | [`plans/acceptance-test-fixture_stub.md`](plans/acceptance-test-fixture_stub.md) |
| Source trust metadata | — (new) | [`plans/source-trust-metadata_stub.md`](plans/source-trust-metadata_stub.md) |
| Multi-provider Parts 1 & 2 (Infomaniak + per-agent tiering) | — (new) | [`plans/multi-provider.md`](plans/multi-provider.md) (Part 3 post-v1) |
| `dr review-queue` (Phase 1) | — (new) | [`plans/dr-review-queue.md`](plans/dr-review-queue.md) (Phases 2–3 post-v1) |
| Parent-company inference (post-v1) | — (new) | [`plans/parent-company-inference.md`](plans/parent-company-inference.md) |
| Researcher decomposition (3-step tool-free) | — (new) | [`plans/completed/researcher-decomposition.md`](plans/completed/researcher-decomposition.md) ✓ done |
| Background-job framework / wayback archival | — (new) | [`plans/wayback-archive-job.md`](plans/wayback-archive-job.md) (interim flip done, framework post-v1) |
| `llm-tester` tracked scripts | — (new) | [`plans/completed/llm-tester-refactor.md`](plans/completed/llm-tester-refactor.md) ✓ done |
| Scorer quality signals (fallback fix, publisher hints, parent_company) | — (new) | [`plans/completed/scorer-quality-signals.md`](plans/completed/scorer-quality-signals.md) ✓ done |
