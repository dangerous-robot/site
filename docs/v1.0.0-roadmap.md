# v0.1.0 Roadmap

**Status**: In progress — sprint planning doc  
**Last updated**: 2026-04-23  
**Detail research**: [`drafts/v0.1.0-mvp-definition.md`](drafts/v0.1.0-mvp-definition.md)

Status key: `done` | `in progress` | `ready` (plan exists, not started) | `plan needed` | `decision needed`

---

## Release criteria checklist

The site can be tagged `v0.1.0` when all of these are true:

- [x] Criteria curated to 6-template active set (3 company, 3 product) in `templates.yaml`
- [x] "Standards" → "Criteria" rename live (display + schema + route) — `/criteria/` route, `criteria` collection, `criteria_slug` field all landed
- [x] All existing claims backfilled to `status: published` (17 files, 2026-04-22)
- [x] `status` field schema implemented and pipeline writes `status: draft` on new claims — schema at `content.config.ts:160`, pipeline writes draft in `persistence.py`
- [ ] Curated claim set published: Earthly Insight removed ✓, TreadLightlyAI COI claims deleted ✓, ChatGPT description filled in (still `description: ''`)
- [x] `existential-safety-score` moved to sector scope — now under `sectors/ai-llm-producers`
- [ ] COI disclosure on FAQ or `/methodology` page
- [ ] COI notice on `companies/treadlightlyai` entity page (inline, not just methodology page)
- [x] GitHub repo correctly linked in footer
- [ ] README expanded to be minimally useful to an external visitor (still 8 lines)
- [ ] At least one GitHub issue template live (source submission or claim request)
- [ ] No `unverified` + `confidence: low` claims published for the operator's own company without disclosure
- [ ] `dr lint` CI integration job in `ci.yml`
- [ ] CI check enforces `human_review.reviewed_at` non-null on all `status: published` claims (no branch-protection-only workaround)
- [ ] Audit trail CI gates: staleness check (verdict mismatch fails build) + orphan sidecar check
- [ ] Pre-launch quick fixes plan complete ([`pre-launch-quick-fixes.md`](pre-launch-quick-fixes.md)) — bundles ALPHA banner, `/values`, audit sidecar `models_used`, `dr research` rename, Citation Auditor rename, Page Builder removal, etc.
- [ ] Multi-provider Part 1: one `dr verify` demo run against Infomaniak `gpt-oss-120b` ([`multi-provider.md#part-1----v1-release-switch-to-infomaniak-gpt-oss-120b`](multi-provider.md#part-1----v1-release-switch-to-infomaniak-gpt-oss-120b))
- [x] Pipeline fail-fast for 403/401 (`TerminalFetchError`) — already implemented
- [x] Timeout centralization (`pipeline/common/timeouts.py`) — already implemented
- [x] Source dedup in onboard (`onboard-reuse-verify-sources`) — already implemented
- [x] Workflow paragraphs and design principle landed in `AGENTS.md` and `glossary.md` (commit `7943577`)
- [x] Orchestrator + Router documented in `glossary.md` Roles + Workflow + Agent-tasks tables (Item 3 (a) split) (commit `7943577`)
- [x] `docs/plans/triage-agent.md` promoted from drafts (2026-04-26); V1 surface implementation done (commit `020409f`): Orchestrator docstring naming + threshold-in-Orchestrator wiring. Full Router still post-v1.
- [x] `docs/plans/claim-lifecycle-states.md` promoted from drafts (2026-04-26); implementation done (commit `df7537e`): `phase` field, `blocked_reason` field, `'blocked'` status enum, `>= 2` threshold.
- [x] `docs/plans/multi-topic.md` promoted from drafts (2026-04-26); schema and code implementation done (commit `1394bc6`): `category` → `topics` rename, array cardinality (1–3), templates strawman in place, acceptance tests skipped via marker. **Operator action remaining**: regenerate the 17 deleted claim files via `dr` CLI before tagging.
- [x] Object lifecycle (general four-state) documented in `glossary.md` (commit `7943577`)
- [x] Updated claim lifecycle (`in-progress`, `blocked`, threshold ≥ 2) documented in `glossary.md` (commit `7943577`)

v0.1.0 is **not** blocked by: audit trail UI (append-only recheck history), public feedback backend, pipeline diagram, or multiple external reviewers.

---

## 0. Criteria curation

**Status**: `done`  
**Detail**: [`drafts/v0.1.0-mvp-definition.md §0`](drafts/v0.1.0-mvp-definition.md)

`research/templates.yaml` already has 6 active templates (3 company, 3 product) and 13 deferred under `inactive_templates`. No code changes needed.

---

## 1. "Standards" → "Criteria"

**Status**: `done` (landed in commit 3282079, "criteria rename Phase 1")  
**Plan**: [`criteria-rename_stub.md`](criteria-rename_stub.md)

`/criteria/` route exists, `criteria` collection registered in `src/content.config.ts:207`, `criteria_slug` field in place. Schema-level rename complete. Any residual `standard_slug` references in claim bodies should be caught by `dr lint`.

---

## 2. Claim status / release flag

**Status**: `done` (landed in commits 73f40d2, 9d242ab)  
**Plan**: [`claim-status_stub.md`](claim-status_stub.md)

Schema has `status: z.enum(['draft', 'published', 'archived']).default('draft')` (`content.config.ts:160`). Verdict enum extended with `not-applicable`. All 17 existing claims backfilled to `status: published`. Pipeline writes `status: draft` on new claims. Verify before tagging: public list pages actually filter to `published` and direct URLs render non-published claims with a status notice.

---

## 3. Curated v0.1.0 claim set

**Status**: `in progress` (most preconditions cleared; one content item and verifications remain)  
**Detail**: [`drafts/v0.1.0-mvp-definition.md §3`](drafts/v0.1.0-mvp-definition.md)

**Done**:
- `companies/earthly-insight` removed.
- TreadLightlyAI pipeline-misresolved claims removed (no `research/entities/companies/treadlightly*` present).
- `greenpt` entity file created (`research/entities/companies/greenpt.md` exists).
- `sectors/ai-llm-producers` entity created (item 4 precondition).

**Remaining**:
- Fill in `description` for `products/chatgpt` (currently `description: ''`).
- After rename: verify `grep -r 'standard_slug:' research/claims/` returns zero results.
- Flagship claim audit: walk the §3 list from `drafts/v0.1.0-mvp-definition.md` and confirm each resolves against the current schema (post-rename, post-sector).

---

## 4. Sector-wide claims

**Status**: `done` (landed in commit 33c8ad3)  
**Plan**: [`sector-claims.md`](sector-claims.md)

`sector` added to entity `type` enum. `research/entities/sectors/ai-llm-producers.md` created. `existential-safety-score` moved out of `anthropic/` into sector scope. A dedicated `/sectors/` route remains post-v0.1.0; cross-links from company pages via `/claims` are the v0.1.0 surface.

---

## 5. Human sign-off

**Status**: `ready`  
**Plans**: [`drafts/human-signoff-research.md`](drafts/human-signoff-research.md), [`drafts/human-signoff-identity-research.md`](drafts/human-signoff-identity-research.md), [`audit-trail.md`](audit-trail.md), [`claim-promotion-audit-rename-verdicts.md §1`](claim-promotion-audit-rename-verdicts.md)

**v0.1.0 minimum** (no backend required):
- `dr review --approve` flips `status: draft` to `published` and writes the audit sidecar in one step (sidecar-first ordering). `dr review --archive` retires a published claim to `status: archived`. Bare `dr review` writes only the sidecar. Implemented; see claim-promotion plan §1.
- PR workflow recommended but not required for solo operator
- GitHub issue template for source submission (`.github/ISSUE_TEMPLATE/submit-source.md`)
- GitHub issue template for claim requests (`.github/ISSUE_TEMPLATE/request-claim.md`)
- COI disclosure on FAQ and/or methodology page

**Future** (post-v0.1.0):
- v0.2: ORCID OAuth for external reviewer identity
- v0.3: IndieAuth, Mastodon/Bluesky as additional identity options
- public-participation-forms plan: web forms for source submission, claim requests, standard proposals

---

## 6. Pipeline diagram

**Status**: `ready` (design task, deferred from v0.1.0 blocker list)  
**Plan**: [`pipeline-diagram_stub.md`](pipeline-diagram_stub.md)  
**Spec**: [`drafts/v0.1.0-mvp-definition.md §6`](drafts/v0.1.0-mvp-definition.md)

Static SVG or Mermaid flowchart showing: Human Question → Researcher → Ingestor → Analyst → Auditor → Human Review → Published Claim. Human checkpoints in accent color. Placement: inside the FAQ "What methodology is used for research?" accordion. SVG preferred for CSS token compatibility (light/dark/high-contrast).

Not a v0.1.0 blocker — the FAQ text describes the pipeline adequately for now.

---

## 7. Fit and finish

**Status**: `in progress`  
**Detail**: Full 13-item checklist in [`drafts/v0.1.0-mvp-definition.md §7`](drafts/v0.1.0-mvp-definition.md)

**Critical for v0.1.0**:
- [ ] COI disclosure (TreadLightly AI relationship) — not on site anywhere
- [ ] GitHub issue templates (source submission, claim request)
- [ ] README expanded — currently 9 lines, no project description
- [ ] CONTRIBUTING.md — add sections for source submission, claim requests, `dr` CLI overview
- [ ] Footer: add license link, methodology/FAQ link, feedback/contact link
- [ ] `chatgpt` entity description — currently empty

**Lower priority** (post-v0.1.0):
- [ ] Per-page `description` meta tags on list pages
- [ ] `/topics` rendering verification
- [ ] Source submission mailto fallback for users without GitHub

---

## 9. Content linter (`dr lint` static scan)

**Status**: `ready`  
**Plan**: [`dr-lint.md`](dr-lint.md)

Run `dr lint` locally before the v0.1.0 tag to catch orphaned claims, missing required fields, broken cross-references, and placeholder values. No LLM, no network — fast static scan, exits 1 on errors.

CI integration is not required for v0.1.0 but follows naturally once the static scan is stable.

---

## 10. Claim promotion, `dr reassess` rename, verdict definitions

**Status**: `in progress` — implemented in working tree, not yet committed  
**Plan**: [`claim-promotion-audit-rename-verdicts.md`](claim-promotion-audit-rename-verdicts.md)

- **§1 Claim promotion**: `dr review --approve` / `--archive` flags implemented in `pipeline/orchestrator/cli.py:530-606`; tests added to `pipeline/tests/test_audit_trail.py`. Uncommitted.
- **§2 Rename**: `dr reassess` already the function name (`cli.py:199`); `AGENTS.md`, `research-workflow.md`, and the CLI smoke test reference it. Needs a final grep sweep for stragglers.
- **§3 Verdict definitions**: `glossary.md`, `analyst/instructions.md`, `auditor/instructions.md` all modified; uncommitted.

**Next**: commit these changes, then use `dr review --approve` on the first claim to produce the first real `.audit.yaml` sidecar (none exist yet under `research/claims/`).

---

## 11. Pre-launch quick fixes

**Status**: `ready`
**Plan**: [`pre-launch-quick-fixes.md`](pre-launch-quick-fixes.md)
**Triage source**: [`../pre-launch-triage.md`](../pre-launch-triage.md) (dispatch ledger; deletable once destinations are reachable)

A bundle of small reader-facing and operator-vocabulary fixes from the 2026-04-24 pre-launch triage. Each item is shippable in under a day. Highlights:

- ALPHA banner; `/values` editorial page; pipeline diagram on FAQ/methodology
- Audit sidecar `models_used` field (lock schema **before** the first sidecar is written)
- Reader-takeaway line under each verdict badge
- Rename "Citation Auditor" → "citation check"; rename `dr research` → `dr verify-claim`
- Remove "Page Builder" role (operator: causing confusion)
- Document model-tier discipline; confidence rubric on `/methodology`

Larger items from the same triage have their own plans:

- [`v0.1.0-vocab-workflow-landing.md`](v0.1.0-vocab-workflow-landing.md) — full vocabulary cohesion pass (absorbed and supersedes the deleted `vocab-rename-pass_stub.md`)
- [`acceptance-test-fixture_stub.md`](acceptance-test-fixture_stub.md) — Anthropic/Claude regression case (Q8)
- [`source-trust-metadata_stub.md`](source-trust-metadata_stub.md) — minimum subset for launch sources
- [`triage-agent.md`](triage-agent.md), [`claim-lifecycle-states.md`](claim-lifecycle-states.md), [`multi-topic.md`](multi-topic.md) — v1.0.0-required schema and workflow plans (promoted from drafts 2026-04-26)

Open questions live at [`../pre-launch-questions.md`](../pre-launch-questions.md).

---

## 12. Multi-provider Part 1 (Infomaniak `gpt-oss-120b`)

**Status**: `ready`
**Plan**: [`multi-provider.md`](multi-provider.md) (Part 1 only)
**Report**: [`../reports/API-PROVIDER-FINAL-REPORT.md`](../reports/API-PROVIDER-FINAL-REPORT.md)

Add `infomaniak:` model-spec support so a single `dr verify` run can target Infomaniak-hosted `gpt-oss-120b`. One canned-claim demo with all four agents producing a written `.audit.yaml` is the bar; per-agent tiering and Mistral-Small support are explicitly Part 2 (post-v1).

Motivation for Infomaniak-first (not GreenPT, despite the report's marginal preference): Infomaniak's data centres reuse waste heat to warm Swiss buildings — a direct fit for TreadLightly's environmental-transparency brand.

Code surface: one new helper (`pipeline/common/models.py:resolve_model`), six `.override(...)` substitutions, and softening the `ANTHROPIC_API_KEY` check in `cli.py`.

---

## 8. Semantic versioning

**Status**: `decision needed`

**Proposal**: `package.json` version field + git tags. `v0.1.0` is the first public release tag. DNS resolution is explicitly not part of the tag criteria — the tag records content/code readiness, not domain availability.

**Version semantics for a content site**:
- **Major**: breaking change to verdict enum, criterion definitions, or entity URL structure
- **Minor**: new entity type, new criterion, new capability shipped (e.g., audit trail, public feedback)
- **Patch**: content corrections, verdict updates, bug fixes, source additions

**Decision needed**: Display version in site footer? (Low effort: `import pkg from 'package.json'; {pkg.version}` in `Base.astro`.)

---

## Backlog cross-reference

| Roadmap item | Backlog phase | Plan file |
|---|---|---|
| Criteria rename | — (new) | [`criteria-rename_stub.md`](criteria-rename_stub.md) |
| Claim status flag | — (new) | [`claim-status_stub.md`](claim-status_stub.md) |
| Curated claim set | — (content work) | this doc |
| Sector claims | — (new) | [`sector-claims.md`](sector-claims.md) |
| Human sign-off / audit trail | audit-trail plan | [`audit-trail.md`](audit-trail.md) |
| Human sign-off identity | reviewer identity plan | [`drafts/human-signoff-identity-research.md`](drafts/human-signoff-identity-research.md) |
| Public participation forms | public participation forms plan | [`public-participation-forms.md`](public-participation-forms.md) |
| Pipeline perf | source dedup / pipeline hardening | [`onboard-reuse-verify-sources.md`](onboard-reuse-verify-sources.md) et al. |
| Public feedback backend | public feedback backend plan | [`public-feedback.md`](public-feedback.md) |
| Fit and finish | — | this doc |
| Pipeline diagram | — (design, deferred) | [`pipeline-diagram_stub.md`](pipeline-diagram_stub.md) |
| Content linter | — (new) | [`dr-lint.md`](dr-lint.md) |
| Claim promotion / `dr reassess` rename / verdict defs | — (new) | [`claim-promotion-audit-rename-verdicts.md`](claim-promotion-audit-rename-verdicts.md) |
| Pre-launch quick fixes | — (new) | [`pre-launch-quick-fixes.md`](pre-launch-quick-fixes.md) |
| Vocabulary + workflow landing (v0.1.0) | — (new) | [`v0.1.0-vocab-workflow-landing.md`](v0.1.0-vocab-workflow-landing.md) |
| Router + Orchestrator naming | — (new) | [`triage-agent.md`](triage-agent.md) |
| Claim lifecycle states | — (new) | [`claim-lifecycle-states.md`](claim-lifecycle-states.md) |
| Multi-topic schema rename | — (new) | [`multi-topic.md`](multi-topic.md) |
| Acceptance test fixture (Anthropic/Claude) | — (new) | [`acceptance-test-fixture_stub.md`](acceptance-test-fixture_stub.md) |
| Source trust metadata | — (new) | [`source-trust-metadata_stub.md`](source-trust-metadata_stub.md) |
| Multi-provider Part 1 (Infomaniak gpt-oss) | — (new) | [`multi-provider.md`](multi-provider.md) |
